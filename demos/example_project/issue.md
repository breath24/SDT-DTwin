# Add greeting function and CLI

Please add a new function `greet(name: str) -> str` in `greet.py` that returns "Hello, {name}!".
Also add a CLI in `main.py` that prints the greeting when run as: `python -m main <name>`.
